docker swarm init --advertise-addr $(docker-machine ip Char)
docker node ls

# init - инициализация этого роя.
# --advertise-addr - тут мы говорим, что вход в этот рой происходит с помощью адреса Char. Единственный флаг который у нас как раз-таки есть

# https://habr.com/ru/company/redmadrobot/blog/318866/
# https://docs.docker.com/engine/swarm/swarm-tutorial/create-swarm/

# Проверка:]
# Рой - это получается система из разных машин, на всех из которых у тебя работает докер. Которые могут общаться друг с другом.
# У Докера есть два режима: 1) В первом он работает только на конкретном компьютере на котором ты работаешь.
# 2) Второй режим - как раз таки Рой - это режим с помощью которого он может говорить со многими компьютерами одновременно
# В этом-то, как я понял, и был смысл создания Виртуальной машины, поскольку с помощью роя
# можно добавить несколько виртуальных машин в рой и потом они буду работать вместе, Но в реальной жизни это может быть 
# не виртуальные машины, а вполне реальные, физические машины.
# Это очень удобно, поскольку можно сделать, чтобы тот, компьютер, который у тебя дома и который в школе могут кооперироваться ]
# с помощью роя
